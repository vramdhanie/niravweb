#!/bin/sh
# Refuse to push anything that would fail CI's lint + type-check phase.
# Bypass in an emergency with: git push --no-verify
set -e
echo "pre-push: eslint…"
pnpm run --silent lint
echo "pre-push: tsc --noEmit…"
pnpm exec tsc --noEmit
echo "pre-push: clean ✓"
